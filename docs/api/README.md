# API Error Documentation

The in-app documentation pages for API error codes are available at:

- `/docs/api`
- `/docs/api/errors`

These pages map each unique code to:

- Meaning and severity
- Canonical message patterns
- Source files
- Debug checklist

Canonical catalog source is defined in `lib/docs/api-error-catalog.ts`.
